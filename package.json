{
  "name": "karsa",
  "version": "1.0.0",
  "private": true,
  "description": "KARSA — pembuat aplikasi berbasis browser (vibecoding)",
  "scripts": {
    "stamp": "node scripts/stamp-version.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "node --check js/ai-core.js && node --check js/ai.js"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
